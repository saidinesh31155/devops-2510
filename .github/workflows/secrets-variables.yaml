name: secrets and variables
on: workflow_dispatch
jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - name: login database
        run: echo "login database using username 'Admin' and password 'Admin123'
  

name: secrets and variables
on: workflow_dispatch
jobs:
  secrets:
    env:
      username: Admin
    runs-on: ubuntu-latest
    steps:
      - name: login database
        run: echo "login database using passowrd 'Admin123' and username $USERNAME"
  

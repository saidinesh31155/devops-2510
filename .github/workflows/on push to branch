name: On Push to specific branch
on:
  push:
    branches:
      - main

jobs:
  name: push to main
  runs on: ubuntu-latest
  steps:
    - name: Run on push to specific branch
      run: echo "push to main branch"
    
